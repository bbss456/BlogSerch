bootJar { enabled = true }
jar { enabled = false }

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation project(':blog-history-domain')
    implementation project(':blog-serch-client')
}